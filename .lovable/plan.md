

## Redeploy Edge Function

**Action:** Redeploy the `linkedin-api` edge function to activate the latest changes (removal of unsupported `submittedAtTimeRange` query parameters from the Lead Form Responses API call).

**Step:** Use the deploy tool to redeploy `linkedin-api`.

